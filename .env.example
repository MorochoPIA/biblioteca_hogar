# Clave secreta para JWT (genera una propia con: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=tu_clave_secreta_aqui

# Credenciales del admin (se crean automáticamente al iniciar)
ADMIN_USER=admin
ADMIN_PASS=cambia_esta_contrasena

# Puerto del servidor (opcional)
PORT=8000
